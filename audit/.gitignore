# Triage artifacts contain real sender addresses and other mailbox state (PII).
# Belt-and-suspenders on top of the repo-root `audit/*` rule: ignore EVERYTHING
# in this directory and any sub-directory except the documentation and this rule.
*
!.gitignore
!README.md
